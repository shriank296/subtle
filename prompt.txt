# prompt customize
source "${HOME}/dotfiles/.shared_prompt"

PS1='$(prompt_venv)'
PS1+="${bold}\n"
PS1+="${userStyle}%n${white} at ${hostStyle}%m"
PS1+="${white} in ${green}%1~"
PS1+='$(prompt_git)'
PS1+="\n${white}$ ${reset}"

export PS1

PS2="${yellow}→ ${reset}"
export PS2
